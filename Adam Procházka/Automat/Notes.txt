Na zapnutí venv:
& "g:/win32app/Portable Python-3.13.3 x64/python.exe" -m venv venv
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
Maturitni_projekty_EP4B_1.pol\.\venv\Scripts\activate

Musí se stáhnout Pygame přes: pip install Pygame
A musí se stáhnout flask přes: pip install flask
!!! Pokud to nebude fungovat kvůli venvu (fatal error), zkus: python -m pip install flask/python

Udělat requirements u installu ať je to jednodušší?
Opravit stats a leaderboard (loading & saving)
Přidat další symboly?
Dodělat grafiku hry (nějaký pozadí?)
Opravit a upravit ty chipy a shop

Případně nastavit path:
cd "F:\Maturitni_projekty_EP4B_1.pol\Adam Procházka\Automat"

Pokud nebude build.bat fungovat, je nutný změnit cestu a verzi pythonu
CELKOVĚ POZOR NA VERZE PYTHONU!